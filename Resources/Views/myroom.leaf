#extend("profile"):
#export("profileContent"):
<div class="bg-light shadow-md rounded p-4">
    <!-- Favourites
  ============================================= -->
    <h4 class="mb-0">Your Properties</h4>
    <div class="d-flex justify-content-end mb-3">
    <form action="/rooms/new">
       <button class="btn btn-sm btn-warning shadow-none text-nowrap"
                                type="submit">Add Rent</button> 
    </form>
                            
                        </div>
    <div class="table-responsive-lg">
        <table class="table table-hover border">
            <tbody>
              
                #for(item in items):
                    #extend("room")
                #endfor
            </tbody>
        </table>
    </div>
    <!-- Favourites end -->

</div>
#endexport
#endextend
