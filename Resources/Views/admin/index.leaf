<!DOCTYPE html>
<html lang="en">
  #extend("head")
  <body class="hold-transition sidebar-mini layout-fixed">
    <div class="wrapper">
      #extend("preloader")
      
      #extend("navbar")
      #extend("mainSidebar")
      #extend("content")
      #extend("footer")
    </div>
  #extend("scripts")
  </body>
</html>
